# tf

```
tf <any terraform command>
tf aa                       - apply -auto-approve
tf df                       - destroy -force
tf aa:recurse               - apply current dir and sub-dirs starting from the current dir.
tf df:recurse               - destroy all the dirs starting from the deepest dir and working back up to the current directory
```
